name: Deploy CF ServiceOps — DISABLED

on:
  workflow_dispatch:

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        shell: bash
        run: |
          echo 'Google Apps Script deployment is intentionally disabled in this branch.'
          echo 'GitHub is source control only. The Sheet-bound Apps Script project must remain in place.'
          echo 'A future live deployment requires a separate explicit decision and authenticated clasp setup.'
