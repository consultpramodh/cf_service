name: Expand exact v5.8.0 release — DISABLED

on:
  workflow_dispatch:

jobs:
  expansion-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Expansion intentionally disabled
        run: |
          echo 'Automatic archive expansion is disabled.'
          echo 'The exact v5.8.0 ZIP under releases/ is the authoritative complete release artifact.'
          echo 'No Google Apps Script deployment is performed by this workflow.'
